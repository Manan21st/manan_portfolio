import './Skills.css';

function Skills(){
    return (
        <div className="skills">
            <h1>Skills</h1>
            <p>HTML, CSS, JS, React, Node, Express, MongoDB, etc.</p>
        </div>
    );
}

export default Skills;